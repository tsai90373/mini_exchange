# CLAUDE.md

Project-level instructions for Claude Code working in this repo.

## Agent skills

### Issue tracker

GitHub Issues on `RyanTsai90373/mini_exchange` via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Default canonical names — no overrides. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context. `CONTEXT.md` at root (lazy), ADRs in `docs/adr/`, live design in `DESIGN.md`. See `docs/agents/domain.md`.
